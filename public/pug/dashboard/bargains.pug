
extends ../layouts/dashboardLayout.pug

block title 
    | الصفقات
block content
    .bargains-page
        +pageHead('فلتر الصفقات')
        .page-filters-2
            .inputs-btn
                +select({label:'' , options:['الأحدث' , 'إختر' , 'إختر' , 'الأحدث']})
                +select({label:'' , options:['حسب القسم' , 'إختر' , 'إختر' , 'حسب القسم']})
                +Button({color:'orange' , name:'التالي ' , class:'ml-2 go-to-step-3 btn-fill' })
            .table-search-box 
                input(type="text" class="form-control")
                .icon 
                    svg
                        use(href="../../assets/images/icons/icons.svg#search")
        .row.row-cols-md-2.row-cols-lg-3.row-cols-xl-3.match-height 
            .col 
                +bargainsBox({progress:'bg-success'})
            .col   
                +bargainsBox({progress:'bg-danger', isDanger:true})
            .col   
                +bargainsBox({progress:'bg-orange'})
            .col   
                +bargainsBox({progress:'bg-orange'})
            .col 
                +bargainsBox({progress:'bg-success'})
            .col   
                +bargainsBox({progress:'bg-danger', isDanger:true})
            .col   
                +bargainsBox({progress:'bg-orange'})
            .col   
                +bargainsBox({progress:'bg-orange'})
            .col 
                +bargainsBox({progress:'bg-success'})
            .col   
                +bargainsBox({progress:'bg-danger', isDanger:true})
            .col   
                +bargainsBox({progress:'bg-orange'})
            .col   
                +bargainsBox({progress:'bg-orange'})
            .col 
                +bargainsBox({progress:'bg-success'})
            .col   
                +bargainsBox({progress:'bg-danger', isDanger:true})
            .col   
                +bargainsBox({progress:'bg-orange'})
            .col   
                +bargainsBox({progress:'bg-orange'})